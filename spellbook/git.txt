== Create a branch without ancestry ==

$ git symbolic-ref HEAD refs/heads/newbranch
$ git rm --cached -r .

Optionally, clean the working directory:
$ git clean -f -d

Then git add some files and git commit, or git commit --allow-empty to
create a completely empty branch.

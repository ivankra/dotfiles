Gnome-style integrated window buttons are sized in pixels and come out tiny
on HiDPI; size them in points like the Windows style.

diff --git a/wezterm-gui/src/termwindow/render/window_buttons.rs b/wezterm-gui/src/termwindow/render/window_buttons.rs
index d8bf607..2b2ac30 100644
--- a/wezterm-gui/src/termwindow/render/window_buttons.rs
+++ b/wezterm-gui/src/termwindow/render/window_buttons.rs
@@ -185,7 +185,7 @@ mod gnome {
     }];
 
     pub fn sized_poly(poly: &'static [Poly]) -> SizedPoly {
-        let size = Dimension::Pixels(8.);
+        let size = Dimension::Points(6.);
         SizedPoly {
             poly,
             width: size,
@@ -285,8 +285,8 @@ pub fn window_button_element(
                 })
         }
         Style::Gnome => {
-            let dim = Dimension::Pixels(7.);
-            let border_corners_size = Dimension::Pixels(12.);
+            let dim = Dimension::Points(5.25);
+            let border_corners_size = Dimension::Points(9.);
             element
                 .zindex(1)
                 .vertical_align(VerticalAlign::Middle)
@@ -296,7 +296,7 @@ pub fn window_button_element(
                     top: dim,
                     bottom: dim,
                 })
-                .border(BoxDimension::new(Dimension::Pixels(1.)))
+                .border(BoxDimension::new(Dimension::Points(0.75)))
                 .border_corners(Some(Corners {
                     top_left: SizedPoly {
                         width: border_corners_size,

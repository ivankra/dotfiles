diff --git a/dev.zed.Zed.yaml b/dev.zed.Zed.yaml
index 79bcabb..9b05194 100644
--- a/dev.zed.Zed.yaml
+++ b/dev.zed.Zed.yaml
@@ -15,7 +15,7 @@ cleanup:
   - /share/man
 finish-args:
   - --device=dri
-  - --filesystem=home
+  #- --filesystem=home
   - --share=ipc
   - --share=network
   - --socket=fallback-x11
@@ -23,15 +23,17 @@ finish-args:
   - --socket=wayland
 
   # Enable access to Flatpak host commands (Needs linter exception: https://docs.flathub.org/docs/for-app-authors/linter#exceptions)
-  - --talk-name=org.freedesktop.Flatpak
+  #- --talk-name=org.freedesktop.Flatpak
 
   # Enable access to Freedesktop Secret Service & related auth services since Zed uses Git heavily
-  - --talk-name=org.freedesktop.secrets
-  - --socket=ssh-auth
-  - --socket=gpg-agent
+  #- --talk-name=org.freedesktop.secrets
+  #- --socket=ssh-auth
+  #- --socket=gpg-agent
 
   - --env=SSH_ASKPASS=/app/libexec/openssh/gnome-ssh-askpass
   - --env=ZED_UPDATE_EXPLANATION="Please use flatpak to update zed"
+  - --env=ZED_FLATPAK_NO_ESCAPE=1
+  - --env=FLATPAK_ENABLE_SDK_EXT=*
 
 modules:
   - shared-modules/libsecret/libsecret.json

#!/bin/bash
# Default startup script called if local repository has no local startup.sh

#if [[ -d ~/.config/chromium ]]; then
#  (set -x; ~/.dotfiles/browser/tweak-chromium.py)
#fi
#
#if [[ -d ~/.mozilla/firefox ]]; then
#  (set -x; ~/.dotfiles/browser/tweak-firefox.py)
#fi
